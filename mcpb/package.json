{
  "name": "omitly-leak-check-mcpb",
  "private": true,
  "version": "0.1.15",
  "description": "MCPB bundle (source) for Omitly Redaction Leak Check — the free, diagnosis-only tier, packed to a one-click Claude Desktop extension. Not published to npm; packed with `npm run mcpb:pack` (see ../package.json).",
  "main": "server/index.js",
  "license": "SEE LICENSE IN ../LICENSE",
  "scripts": {
    "test": "node --test server/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
